import React from 'react';
import { useCart } from 'react-use-cart';


export default function Address() {
    const { items } = useCart();
    return (
        <div>
            
        </div>
    )
}
